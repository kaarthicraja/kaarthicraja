# 🏗️ Architecture & System Documentation

## Overview

This repository is the source of truth for the **kaarthicraja** GitHub profile. It is structured as a fully modular, self-updating system where static and dynamically generated assets are cleanly separated.

---

## 📁 Folder Structure

```
kaarthicraja-kaarthicraja/
├── assets/
│   ├── animations/         # All hand-crafted SMIL SVG animations
│   ├── icons/              # Tech stack icon set (10 icons, neon glow)
│   └── generated/          # Auto-generated by GitHub Actions (DO NOT EDIT)
│       ├── quote.svg                              ← daily quote
│       ├── metrics.svg                            ← GitHub metrics
│       ├── github-contribution-grid-snake.svg     ← light mode snake
│       └── github-contribution-grid-snake-dark.svg← dark mode snake
├── scripts/
│   └── generateQuote.js    # Node.js: reads quotes.json → writes quote.svg
├── .github/
│   └── workflows/
│       ├── quote.yml       # Runs generateQuote.js daily
│       ├── snake.yml       # Platane/snk — contribution grid snake
│       └── metrics.yml     # lowlighter/metrics — profile metrics
├── docs/
│   └── ARCHITECTURE.md     # This file
├── quotes.json             # Pool of programming quotes
├── package.json            # Node project manifest
├── .gitignore
├── LICENSE                 # MIT
└── README.md               # The public-facing GitHub profile
```

---

## 🔄 Automation Workflows

### 1. `quote.yml` — Daily Quote Generator

| Property | Value |
|----------|-------|
| Trigger  | Daily `cron: '0 0 * * *'` + `workflow_dispatch` |
| Auth     | `GITHUB_TOKEN` (built-in, no secrets needed) |
| Output   | `assets/generated/quote.svg` |
| Commit   | Bot commits with `[skip ci]` to prevent recursion |

**Flow:** Cron fires → Node 18 setup → `node scripts/generateQuote.js` → reads `quotes.json` → picks random quote → generates styled SVG → if changed, bot commits and pushes.

---

### 2. `snake.yml` — Contribution Grid Snake

| Property | Value |
|----------|-------|
| Trigger  | Push to `main` + daily `cron: '0 1 * * *'` + `workflow_dispatch` |
| Action   | `Platane/snk@v3` (official snake generator) |
| Auth     | `GITHUB_TOKEN` |
| Output   | `assets/generated/github-contribution-grid-snake.svg` (light) + `...-dark.svg` |
| Commit   | `EndBug/add-and-commit@v9` with bot author |

**Flow:** Trigger fires → `Platane/snk@v3` reads your public contribution calendar from GitHub API → renders snake path SVG → commits both light and dark variants back to `main`.

---

### 3. `metrics.yml` — Advanced GitHub Metrics

| Property | Value |
|----------|-------|
| Trigger  | Push to `main` + daily `cron: '0 2 * * *'` + `workflow_dispatch` |
| Action   | `lowlighter/metrics@latest` |
| Auth     | `GITHUB_TOKEN` (public data only) |
| Output   | `assets/generated/metrics.svg` |
| Plugins  | languages, activity, habits |

**Flow:** Trigger fires → `lowlighter/metrics` reads your public GitHub profile data → renders a comprehensive SVG dashboard showing top languages, recent activity, and commit habits → commits `metrics.svg` back to `main`.

> **Note:** To unlock private repo data or additional plugins (sponsors, etc.), create a PAT with `read:user` + `repo` scopes and store it as a repo secret named `METRICS_TOKEN`, then replace `${{ secrets.GITHUB_TOKEN }}` in `metrics.yml`.

---

## 🎨 SVG Animation System

All SVGs in `assets/animations/` use **SMIL (Synchronized Multimedia Integration Language)** animations — the native SVG animation standard. This ensures:

- ✅ **GitHub compatible** — GitHub's Markdown renderer allows SMIL but strips JavaScript
- ✅ **Zero dependencies** — no JS runtime, no CSS frameworks
- ✅ **Lightweight** — each file is under 15KB

### Animation Inventory

| File | Purpose | Key Animation |
|------|---------|---------------|
| `assets/banner.svg` | Hero dossier header | Underline sweep, scanline, status pulse |
| `animations/typing.svg` | Cycling role titles | Clip-path typewriter reveal |
| `animations/skills.svg` | Tech stack data grid | Per-cell status dot pulse |
| `animations/dashboard.svg` | Engineering metrics panel | Bar fill, live counters, commit bars |
| `animations/pipeline.svg` | CI/CD flow diagram | `<animateMotion>` dot traversal, stage highlight |
| `animations/project-card.svg` | Project showcase card | Status dot pulse |
| `animations/timeline.svg` | Career log | Marker pulse |
| `animations/social-icons.svg` | Contact link grid | Border pulse cycle |

There is deliberately no terminal simulation, no rotating globe, and no footer wave — they didn't fit the disciplined, single-accent industrial-dossier direction and were cut in the second redesign pass (2026-08).

---

## 🧩 Modularity Principle

Each component is designed to be **independently replaceable**:

- To swap the banner: replace `assets/banner.svg`
- To add a new quote: add an object to `quotes.json`
- To change the color scheme: find/replace the palette across `assets/` (see Design System below)
- To add a new icon: drop an SVG into `assets/icons/` and reference it in `README.md`

The `assets/generated/` folder should **never be manually edited** — its contents are always overwritten by the automation workflows.

---

## 🎨 Design System

Industrial/tactical-telemetry direction: near-black background, sharp 90° corners (no `rx`), solid 1px hairline borders instead of glow/blur, monospace-first typography, and exactly **one** accent color.

| Token | Value | Usage |
|-------|-------|-------|
| Background | `#0a0a0a` | SVG backgrounds |
| Panel | `#141414` | Cards, bars, cells |
| Border | `#2a2a2a` | Hairline dividers, frames |
| Text (primary) | `#eaeaea` | Headlines, values |
| Text (secondary) | `#8a8a8a` | Labels, metadata |
| Text (muted) | `#555555` | Micro-labels, dividers |
| Accent (amber) | `#e8a33d` | The one accent — bars, rules, active states |
| Status green | `#4af626` | Single-use only — the "available"/"live" dot, nowhere else |
| Font | `JetBrains Mono` / `IBM Plex Mono`, monospace | All SVG text |

Do not introduce a second accent color or reintroduce `border-radius`/blur filters — that mix is what made the first redesign pass read as generic AI output.
